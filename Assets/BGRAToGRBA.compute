#pragma kernel CSMain

Texture2D<float4> Source;
RWTexture2D<float4> Result;

[numthreads(8,8,1)]
void CSMain(uint3 id : SV_DispatchThreadID)
{
    float4 c = Source[id.xy];
    // ZED's left-view texture is stored BGRA; swap R and B back.
    Result[id.xy] = c.bgra;
}